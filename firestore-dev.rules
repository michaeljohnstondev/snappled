rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads/writes for development testing
    // NEVER use this in production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}